import styled from 'styled-components';

export const HomeWrapper = styled.div`
  width: 100%;

  padding: ${p => p.theme.spacing(1)};
  margin: ${p => p.theme.spacing(1)} 0;
`;

export const WelkomTitle = styled.h2`
  width: 100%;

  font-size: 20px;

  padding: ${p => p.theme.spacing(1)};
  margin: ${p => p.theme.spacing(1)} 0;
`;
